services:
  - type: web
    name: feed-the-world
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      # Ongeza hapa environment variables kutoka server/.env.example
      # mfano: - key: ANTHROPIC_API_KEY
      #          sync: false
